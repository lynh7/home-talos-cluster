apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vaultwarden-kubernetes-secrets.fullname" . }}-config
  labels:
    app.kubernetes.io/name: {{ include "vaultwarden-kubernetes-secrets.name" . }}
    app.kubernetes.io/part-of: {{ include "vaultwarden-kubernetes-secrets.name" . }}
data:
{{- range $key, $value := .Values.env.config }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
